using DistributionSystem.UI.Forms;
using DistributionSystem.UI.Services;
using System;
using System.Windows.Forms;

namespace DistributionSystem.UI
{
    internal static class Program
    {
        [STAThread]
        static void Main()
        {
            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);

            try
            {
                // Perform startup validation; if validation fails, show friendly message and exit
                StartupValidator.ValidateOrThrow();
            }
            catch (Exception ex)
            {
                MessageBox.Show(ex.Message, "Startup Error", MessageBoxButtons.OK, MessageBoxIcon.Error);
                return;
            }

            Application.Run(new LoginForm());
        }
    }
}

namespace DistributionSystem.UI.Forms
{
    using System.Windows.Forms;

    // Minimal stub so Program can reference LoginForm when project file may not include other form sources
    public class LoginForm : Form
    {
        public LoginForm()
        {
        }
    }
}

namespace DistributionSystem.UI.Services
{
    // Minimal stub for startup validation used at program start
    public static class StartupValidator
    {
        public static void ValidateOrThrow()
        {
            // no-op validation stub
        }
    }
}
